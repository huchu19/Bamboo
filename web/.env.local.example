# Firebase Configuration — copy to .env.local and fill in
NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_auth_domain
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_storage_bucket
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=your_measurement_id

# Stripe — get test keys at https://dashboard.stripe.com/test/apikeys.
# While these are *_stub, the invest flow uses the demo simulation instead.
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_stub
STRIPE_SECRET_KEY=sk_test_stub
# From `stripe listen --forward-to localhost:3000/api/stripe/webhook` (dev)
# or the webhook endpoint config in the Stripe dashboard (prod).
STRIPE_WEBHOOK_SECRET=

# Stripe Connect payout destination for investment funds (acct_...).
# While this is the acct_stub placeholder, investment PaymentIntents are
# created WITHOUT transfer_data and funds stay on the platform account.
# Activation steps: MILESTONES.md §6.2.
STRIPE_CONNECT_ACCOUNT_ID=acct_stub

# Firebase Admin SDK (server routes/webhooks). Optional locally — falls back to
# scripts/service-account.json. REQUIRED on Vercel: full service-account JSON
# as a single line.
# FIREBASE_SERVICE_ACCOUNT_KEY=

# Dev bypass — true for local demo iteration, MUST be false in production.
# Vercel prod sets this to false; preview deploys can override per-env.
NEXT_PUBLIC_DEV_BYPASS_AUTH=true

# Invite-only beta gate — true in production for the 100-participant launch.
# Registration then requires a code minted with scripts/generate-invites.mjs.
# For hard (rules-level) enforcement, also enable the inviteRedemptions clause
# in firestore.rules (see the comment on users.create) and redeploy rules.
NEXT_PUBLIC_INVITE_REQUIRED=false

# Sentry error monitoring (Phase 7.3) — inert while the DSN is empty.
# Create a Next.js project at https://sentry.io, paste its DSN here (it is
# safe to expose). SENTRY_ORG/PROJECT/AUTH_TOKEN are build-time only, used to
# upload source maps on Vercel — set them in the Vercel env, not here.
NEXT_PUBLIC_SENTRY_DSN=
# SENTRY_ORG=
# SENTRY_PROJECT=
# SENTRY_AUTH_TOKEN=

# Public site URL — used for OG/Twitter image absolute URLs and canonical links.
# Vercel auto-provides VERCEL_URL as a fallback for previews; set this explicitly in prod.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
